{
  "name": "claudelinked",
  "version": "0.1.0",
  "description": "Let Claude Code sessions on different PCs ask each other questions and deliver answers back, via a small relay and a Claude Code channel.",
  "type": "module",
  "private": true,
  "scripts": {
    "relay": "node relay/server.mjs",
    "test": "node test/e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.5"
  }
}
